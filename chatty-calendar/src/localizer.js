import { Calendar, momentLocalizer } from 'react-big-calendar';
import moment from 'moment';

// Set up the localizer by providing moment (or globalize) to your calendar
const localizer = momentLocalizer(moment);
